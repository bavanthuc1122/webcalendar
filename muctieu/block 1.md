2. Chi tiết từng block
Block 1: Booking Input
Flow: User paste text → click Submit → gọi Gemini API server-side.

UI Spec:

<textarea> full-width, height 120px, border-radius 8px.

Button Submit (primary color #FF5A5F) with spinner animation (200 ms).